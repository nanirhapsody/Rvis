library(dplyr)
library(ggplot2)
library(dslabs)
data(gapminder)

#Esperanza de vida y la guerra de Vietnam parte 2

#Ahora que has creado en el ejercicio anterior la tabla, es momento de graficar los datos
#Utiliza la función geom_line para graficar la esperanza de vida vs el ano para
#Vietnam y los Estados Unidos y guarda este grafico en p.
#Recuerda que el data table lo tienes guardado en tab
#Utiliza colores para distinguir a los dos países
#imprime el objeto p
p <- # Código para el grafico va aquí - la tabla de datos esta guardada en `tab`
  p
