library(dplyr)
library(ggplot2)
library(dslabs)
data(gapminder)

#En el grafico del ejercicio anterior de puede notar una gran variabilidad en todo África
#entre la esperanza de vida (life expectancy) y la fertilidad (fertility) 
#pero también se ven que hay 3 clusters o regiones en el gráfico.
#Colorea ahora los puntos de acuerdo a la región en África donde se encuentran
#para ver si esto explica las 3 diferentes situaciones o clusters.

#Realiza el grafico anterior pero utilizando colores para enmarcar las 
#diferentes regiones.
#Recuerda que puedes utilizar tanto la forma col como color ambos producirán
#el mismo resultado
